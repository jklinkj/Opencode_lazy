# 学院 OpenCode 自定义目录

本目录通过 `OPENCODE_CONFIG_DIR` 注入，用于承载学院统一下发的扩展内容。

V1 先只保留目录骨架，后续可以按需要加入：

- `agents/`
- `commands/`
- `modes/`
- `plugins/`
- `skills/`
- `tools/`
- `themes/`

目前仅提供 `instructions/` 示例内容。
